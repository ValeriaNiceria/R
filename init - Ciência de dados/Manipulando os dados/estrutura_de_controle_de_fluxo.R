# Estrutura de controle de fluxo
# If e Else
variavel <- 100
ifelse(variavel >= 500, 'executa essa tarefa se TRUE', 'executa outra se FALSE')

a <- 839
ifelse(a >= 10000, 'VALOR ALTO', ifelse(a < 10000 & a >= 1000, 'VALOR MEDIO', 'VALOR BAIXO'))

