# Vetores de inteiros que possuem rótulos (levels)
sexo <- c('M', 'H', 'H', 'H', 'M', 'M', 'H')
fator <- as.factor(sexo)
levels(fator)
num <- as.numeric(fator)


# Valores especiais
# is.na()
# is.nan()
# is.infinite()
# is.null()
